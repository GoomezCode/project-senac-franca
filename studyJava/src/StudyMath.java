public class StudyMath {
    static void main() {
        double a,b,c,d,e,f,g;
        a = Math.abs(20);   // retorna valor absoluto (sempre positivo)
        b = Math.pow(3,3); // potenciação
        c = Math.sqrt(25); // raiz quadrada
        d = Math.random()*100; // numero aleatorio
        e = Math.PI;

        System.out.println(e);
    }
}
